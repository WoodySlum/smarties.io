FROM node:12.14-buster

RUN apt-get update
RUN apt-get install -y --allow-unauthenticated systemd vim sqlite3 libmagic-dev libatlas-base-dev libudev-dev
RUN apt-get clean && rm -rf /var/lib/apt/lists/*
